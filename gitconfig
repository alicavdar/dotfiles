[user]
  name = Ali Çavdar
  email = 779832+alicavdar@users.noreply.github.com

[include]
    path = ~/.gitconfig.local

[core]
  pager = delta

[delta]
  line-numbers = true
  side-by-side = true

[color]
  ui = true

[alias]
  upstream = !git push -u origin HEAD
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit

  # Typos I do often
  pll = pull
  plul = pull
  pul = pull
  ad = add
  commmit = commit

[github]
  user = alicavdar

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true

[init]
  defaultBranch = main

[pull]
  rebase = true

[branch]
  autosetuprebase = always

[rebase]
  autostash = true
